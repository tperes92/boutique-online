<section class="banner_area">
  <div class="banner_inner d-flex align-items-center">
    <div class="container">
      <div class="banner_content text-center">
      <h2>Welcome to you Profil Page !</h2>
        <div class="page_link">
        <p> Tu pourras ici, retrouver ton panier et modifier tes informations personnelles, géniale ! </p>
        </div>
      </div>
    </div>
  </div>
</section>

<section class="login_box_area p_120">
  <div class="container">
    <div class="row">

      <div class="col-lg-3">
      </div>

      <div class="col-lg-6">
        <div class="login_form_inner reg_form">

            <h4>Update my informations <br><br></h4>

            <%= form_for(resource, as: resource_name, url: registration_path(resource_name), :html => {:class => 'row login_form', method: :put }) do |f| %>
            <%= render "devise/shared/error_messages", resource: resource %>

            <div class =" col-md-12 form-group ">
            <%= f.label :first_name, 'First Name', :class => 'sr-only' %>
            <%= f.text_field :first_name, autofocus: true, :class => "form-control ", placeholder: 'First Name' %>
            </div>

            <div class =" col-md-12 form-group ">
            <%= f.label :last_name, 'Last Name', :class => 'sr-only' %>
            <%= f.text_field :last_name, autofocus: true, :class => "form-control ", placeholder: 'Last Name' %>
            </div>

            <div class =" col-md-12 form-group ">
              <%= f.label :email, :class => 'sr-only' %>
              <%= f.email_field :email, autofocus: true, autocomplete: "email", :class => "form-control ", placeholder: 'Email Address' %>
            </div>

            <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
              <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
            <% end %>

            <div class =" col-md-12 form-group ">
              <%= f.label :password, :class => 'sr-only' %> 
              <%= f.password_field :password, autocomplete: "new-password", :class => "form-control ", placeholder: "New Password" %>
              <i>(Leave blank if you dont change your password)</i>
            </div>

            <div class =" col-md-12 form-group ">
              <%= f.label :password_confirmation, :class => 'sr-only' %>
              <%= f.password_field :password_confirmation, autocomplete: "new-password", :class => "form-control ", placeholder: 'Confirm New Password' %>
            </div>

            <div class =" col-md-12 form-group ">
              <%= f.label :current_password, :class => 'sr-only' %> <br />
              <%= f.password_field :current_password, autocomplete: "current-password", :class => "form-control ", placeholder: 'Current Password' %>
              <i>(we need your current password to confirm your changes)</i>
            </div>

            <div class =" col-md-12 form-group ">
              <%= f.submit "Update", :class => 'btn submit_btn' %>
            </div>
          <% end %>

          <h4>Cancel my account</h4>

          <p>Unhappy? <%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>

          <%= link_to "Back", :back %>

        </div>
      </div> 

      <div class="col-lg-3">
      </div>

    </div>
  </div>
</section>
